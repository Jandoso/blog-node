<div class="jumbotron">
  <h1 class="display-4">Bem-vindo ao Blog de Node</h1>
  <p class="lead">Este é um blog simples, desenvolvido com Bootstrap, Node.js, Express e MongoDB</p>
  <hr class="my-4">
  <p>Desenvolvido durante o curso básico de Node.js do canal Guia do Programador</p>
  <a class="btn btn-primary btn-lg" href="/usuarios/registro" role="button">Crie uma conta</a>
</div>

<hr>

<h2>Postagens recentes</h2>

{{#each postagens}}
<div class="card mt-4">
    <div class="card-body">
        <h3>{{titulo}}</h3>
        <p>{{descricao}}</p>
        <a href="/postagem/{{slug}}"><button type="button" class="btn btn-success mt-2">Leia mais</button></a>
        <hr>
        <small>Categoria: {{categoria.nome}}</small> <br>
        <small>Data de Publicação: {{data}}</small>
    </div>
</div>
{{else}}
<h5>Nenhuma postagem</h5>
{{/each}}